import { LoadingState, PageContainer } from "@setu/ui";

export default function Loading() {
  return (
    <PageContainer>
      <LoadingState label="Loading operations console" />
    </PageContainer>
  );
}
