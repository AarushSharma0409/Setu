# Insurance evidence

Operations can retrieve immutable consent and disclosure acknowledgement
records by ID. Evidence contains the exact recorded template identifier,
version, status, timestamps, and linked assessment reference. It is
permissioned and audited. The screen does not resolve evidence to current
mutable template content, expose sensitive answers, or create export files.

Evidence export is deferred until a secure artifact-delivery and retention
workflow exists.
