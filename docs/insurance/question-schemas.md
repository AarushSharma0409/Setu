# Question schemas

Question schemas are versioned per policy type and contain ordered sections, typed questions, stable option values, declarative validation, and restrained visibility rules. Published schemas are intended to be immutable; an assessment retains the exact schema version it started with.
