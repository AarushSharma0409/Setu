# Sprint I3 handoff

Enable customer needs only when the I1 operating model permits `COLLECT_CUSTOMER_NEEDS`, effective product/schema/disclosure/consent configuration is present, and both flags are deliberately enabled:

```text
INSURANCE_FEATURE_ENABLED=true
INSURANCE_CUSTOMER_NEEDS_ENABLED=true
```

This sprint does not generate quotes, calculate premiums, call insurers, rank products, or support purchase.
